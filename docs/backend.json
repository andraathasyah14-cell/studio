
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "User account details including administrative privileges.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" },
        "admin": { "type": "boolean", "description": "Whether the user has full CMS access." }
      },
      "required": ["uid", "email", "admin"]
    },
    "Essay": {
      "title": "Essay/Opinion",
      "description": "Core content pieces including drafts and published articles.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "content": { "type": "string" },
        "summary": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "status": { "type": "string", "enum": ["draft", "published", "scheduled", "deleted"] },
        "category": { "type": "string" },
        "authorId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "publishedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "content", "status", "authorId"]
    },
    "Paper": {
      "title": "Academic Paper",
      "description": "Metadata for uploaded PDF research papers.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "author": { "type": "string" },
        "year": { "type": "number" },
        "abstract": { "type": "string" },
        "keywords": { "type": "array", "items": { "type": "string" } },
        "category": { "type": "string" },
        "link": { "type": "string" },
        "uploadedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "author"]
    },
    "Reference": {
      "title": "Reference Source",
      "description": "Citations and sources for the reference bank.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "author": { "type": "string" },
        "source": { "type": "string" },
        "quote": { "type": "string" },
        "link": { "type": "string" },
        "category": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "category"]
    },
    "ActivityLog": {
      "title": "Admin Activity Log",
      "description": "Record of administrative actions.",
      "type": "object",
      "properties": {
        "adminId": { "type": "string" },
        "action": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["adminId", "action", "timestamp"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Store user profiles and admin flags."
    },
    "/essays/{essayId}": {
      "schema": "Essay",
      "description": "Collection of all articles and drafts."
    },
    "/papers/{paperId}": {
      "schema": "Paper",
      "description": "Repository for academic research metadata."
    },
    "/references/{refId}": {
      "schema": "Reference",
      "description": "Bank for important quotes and citations."
    },
    "/activity_logs/{logId}": {
      "schema": "ActivityLog",
      "description": "Audit trail for admin operations."
    }
  }
}
